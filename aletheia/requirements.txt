# --- Noyau (obligatoire) ---
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27
pyyaml>=6.0
python-dotenv>=1.0
itsdangerous>=2.1
python-multipart>=0.0.9

# --- Base de connaissances (RAG) ---
# Par défaut, Aletheia utilise un index mot-clé léger (aucune dépendance lourde),
# parfait pour un premier déploiement sur petite instance.
# Pour une recherche SÉMANTIQUE plus fine, décommente ChromaDB (instance ≥ 1 Go
# recommandée). Le code bascule automatiquement dessus s'il est installé.
# chromadb>=0.5

# --- Ingestion multi-source (optionnel mais recommandé) ---
pypdf>=4.0                 # livres / PDF
beautifulsoup4>=4.12       # pages web
youtube-transcript-api>=0.6  # transcriptions YouTube
